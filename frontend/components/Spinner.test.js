//Spinner.test.js
// Import the Spinner component into this file and test
// that it renders what it should for the different props it can take.
// test('sanity', () => {
//   expect(true).toBe(false)
// })

// import React from'react'
// import { render } from '@testing-library/react'
// import Spinner from './Spinner'

// test('renders a Spinner component when on is true', () => {
//   const { getByText } = render(<Spinner on={true} />)
//   expect(getbyText(/Please wait/i)).toBeInTheDocument()
// })

// test('does not render Spinner component when on is false', () => {
//   const { queryByText } = render(<Spinner on={false} />)
//   expect(queryByText(/Please wait/i)).not.toBeInTheDocument()
// })